[build]
  command = "npm run build"
  publish = "dist"

# Redirect all backend /api/* routes to Netlify Serverless Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
